# Automated Testing Demo (Playwright)

This project demonstrates how I help teams catch bugs before release using automated tests.

## Covered:
- Login flow automation (UI test)
- API response validation

## Why it matters:
- Detect broken login flows or API issues before they affect users
- Quick proof of concept: setup takes less than a week

## Run locally:
```bash
npm install
npx playwright install
npm test
